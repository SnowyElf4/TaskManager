# TaskManager v0.4

Консольный менеджер задач на Java с поддержкой нескольких пользователей и постоянным хранением данных в JSON-файлах.

Проект демонстрирует базовую MVC-подобную архитектуру с разделением:

* пользовательского интерфейса
* бизнес-логики
* слоя хранения данных

## Возможности

* Регистрация пользователей
* Выбор активного пользователя
* Создание задач
* Просмотр всех задач текущего пользователя с описанием и статусом
* Отметка задач как выполненных по ID
* Постоянное хранение пользователей и задач в JSON-файлах
* Каждая задача привязана к конкретному пользователю
* Защита от доступа к чужим задачам
* Безопасный ввод данных с проверкой корректности

## Архитектура

```
src/
 ├─ appconfig/
 │   └─ AppConfig.java
 │
 ├─ repository/
 │   ├─ JsonRepository.java
 │   ├─ TaskRepository.java
 │   └─ UserRepository.java
 │
 ├─ ui/
 │   ├─ controller/ 
 │       ├─ MenuController.java // управление флоу приложения
 │       ├─ TaskController.java // управление задачами
 │       └─ UserController.java // регистрация и выбор пользователя  
 │        
 │   ├─ view/ 
 │       ├─ MenuView.java
 │       ├─ TaskView.java
 │       └─ UserView.java
 │                
 │   └─ InputReader.java        // безопасный ввод данных
 │
 ├─ service/
 │   ├─ IdGenerator.java      
 │   ├─ UserService.java
 │   └─ TaskService.java
 │
 ├─ user/
 │   └─ User.java
 │
 ├─ task/
 │   └─ Task.java
 │    
 └─ Main.java
```

### Ключевые принципы

* `MenuController` управляет логикой переходов между меню
* `MenuView` **не содержит бизнес-логики**, только выводит информацию
* `TaskController` и `UserController` управляют бизнес-логикой отдельных флоу
* `Service` классы не работают с вводом/выводом
* `Repository` управляет данными сущностей
* Данные **не теряются** между запусками приложения

## Установка и запуск

1. Клонировать репозиторий:

```bash
git clone https://github.com/SnowyElf4/TaskManager.git
```

2. Перейти в директорию проекта:

```bash
cd TaskManager
```

3. Скомпилировать проект:

```bash
javac -d out $(find src -name "*.java")
```

4. Запустить приложение:

```bash
java -cp out src.Main
```

## Использование

При запуске:

* если пользователей нет — предлагается регистрация
* если пользователи существуют — можно выбрать активного

### Главное меню

| Команда | Действие                               |
| ------- | -------------------------------------- |
| 1       | Создание задачи                        |
| 2       | Отметка задачи как выполненной (по ID) |
| 3       | Просмотр всех своих задач              |
| 4       | Смена пользователя                     |
| 0       | Выход из приложения                    |

Каждый пользователь видит **только свои задачи**.

### Пример работы

Регистрация пользователя:

```
Write name: SnowyElf
User created.
```

Создание задачи:

```
Write task name: Уборка
Write description: Протереть пыль
Task created.
```

Просмотр задач:

```
ID: 12345678
Task name: Уборка
Description: Протереть пыль
Is done: false
```

Отметка задачи как выполненной:

```
Enter task ID to mark done: 12345678
Task marked as done.
```

## Планы на будущее (v0.5+)

* Редактирование и удаление задач
* Разделение задач на выполненные / невыполненные с фильтром
* Улучшение UX консольного меню
* Обработка некорректного ввода во всех меню
* Подготовка интерфейса хранилища для перехода на БД
* Unit-тесты сервисного слоя
* Возможность сортировки задач по дате создания/выполнения

## Автор

SnowyElf4
